# Optional production API base URL. Defaults to /api/v1 for the Vite development proxy.
VITE_API_BASE_URL=/api/v1

# Optional Vite development proxy target.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
